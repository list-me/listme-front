import { ContainerActionsIntegrationCard } from "./styles";

function ActionsIntegrationCard({ done }: { done: boolean }): JSX.Element {
  return <ContainerActionsIntegrationCard />;
}

export default ActionsIntegrationCard;
